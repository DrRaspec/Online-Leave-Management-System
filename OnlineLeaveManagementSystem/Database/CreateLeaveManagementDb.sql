PRINT 'Database setup is now handled automatically by Infrastructure/DatabaseInitializer.cs at application startup.';
PRINT 'On first startup, the app bootstraps one admin account and writes the temporary password to App_Data/bootstrap-admin.txt.';
